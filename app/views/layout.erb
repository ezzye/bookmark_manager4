<!DOCTYPE html>
<html>
  <head>
    <title>Bookmark Manager</title>
  </head>

  <body>
  <%= flash[:error] if flash[:error] %>
    <% if current_user %>
      Welcome, <%= current_user.username %>
      <% end %>
      <%= yield %>
  </body>
</html>
